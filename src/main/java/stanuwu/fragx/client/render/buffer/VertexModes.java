package stanuwu.fragx.client.render.buffer;

/**
 * Formats a vertex in a buffer builder can have.
 */
public enum VertexModes {
    POSITION_COLOR,
    POSITION_COLOR_TEXTURE,
    POSITION_TEXTURE,
}
